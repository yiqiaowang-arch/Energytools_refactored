# =============================================================================
# Documentation build dependencies (MkDocs + Material + mkdocstrings)
# Shared by local builds and ReadTheDocs (.readthedocs.yaml python.install).
#
# Version policy: fixed major ranges for mkdocs-material cache reuse and
# compatibility. Local verification baseline: mkdocs 1.6.1 +
# mkdocs-material 9.7.7 (Python 3.12).
#
# mkdocstrings renders the API reference directly from the source docstrings
# (the `:::` directives in docs/api-reference/), keeping the symbol reference
# in sync with the code; the handwritten pages under
# docs/architecture+api-reference/ stay usage-oriented and link to the
# generated pages.
# =============================================================================

mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
mkdocstrings[python]>=0.26
