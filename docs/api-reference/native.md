# energytools.engine.native

The ported calculation modules: moist-air physics (Glück polynomials),
the AHU temperature-bin method (`Berechnung LU`) and the building
aggregation chain (`Erzeugung` / `Resultate`). These are low-level building
blocks — the `Engine` normally calls them via a backend.

::: energytools.engine.native
